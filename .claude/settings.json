{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|MultiEdit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path' | { read file_path; if echo \"$file_path\" | grep -q '\\.ts$'; then yarn prettier --write \"$file_path\"; elif echo \"$file_path\" | grep -q '\\.go$'; then goimports -w \"$file_path\"; fi; }"
          }
        ]
      }
    ]
  }
}
