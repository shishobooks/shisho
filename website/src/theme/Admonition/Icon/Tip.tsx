import { Lightbulb } from "lucide-react";
import type { SVGProps } from "react";

export default function AdmonitionIconTip(props: SVGProps<SVGSVGElement>) {
  return <Lightbulb aria-hidden size={18} strokeWidth={2.1} {...props} />;
}
