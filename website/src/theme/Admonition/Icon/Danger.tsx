import { Flame } from "lucide-react";
import type { SVGProps } from "react";

export default function AdmonitionIconDanger(props: SVGProps<SVGSVGElement>) {
  return <Flame aria-hidden size={18} strokeWidth={2.1} {...props} />;
}
