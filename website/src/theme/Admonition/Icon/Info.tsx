import { Info } from "lucide-react";
import type { SVGProps } from "react";

export default function AdmonitionIconInfo(props: SVGProps<SVGSVGElement>) {
  return <Info aria-hidden size={18} strokeWidth={2.1} {...props} />;
}
