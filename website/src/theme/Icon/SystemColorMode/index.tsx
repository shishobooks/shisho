import { CircleDashed } from "lucide-react";
import type { SVGProps } from "react";

export default function IconSystemColorMode(props: SVGProps<SVGSVGElement>) {
  return <CircleDashed aria-hidden size={20} strokeWidth={2.1} {...props} />;
}
