import { WrapText } from "lucide-react";
import type { SVGProps } from "react";

export default function IconWordWrap(props: SVGProps<SVGSVGElement>) {
  return <WrapText aria-hidden size={18} strokeWidth={2.1} {...props} />;
}
