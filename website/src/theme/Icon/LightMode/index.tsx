import { Sun } from "lucide-react";
import type { SVGProps } from "react";

export default function IconLightMode(props: SVGProps<SVGSVGElement>) {
  return <Sun aria-hidden size={20} strokeWidth={2.1} {...props} />;
}
