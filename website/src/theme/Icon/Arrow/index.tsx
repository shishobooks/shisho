import { ChevronsRight } from "lucide-react";
import type { SVGProps } from "react";

export default function IconArrow(props: SVGProps<SVGSVGElement>) {
  return <ChevronsRight aria-hidden size={18} strokeWidth={2.1} {...props} />;
}
