import { Check } from "lucide-react";
import type { SVGProps } from "react";

export default function IconSuccess(props: SVGProps<SVGSVGElement>) {
  return <Check aria-hidden size={18} strokeWidth={2.4} {...props} />;
}
