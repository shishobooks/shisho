import { Moon } from "lucide-react";
import type { SVGProps } from "react";

export default function IconDarkMode(props: SVGProps<SVGSVGElement>) {
  return <Moon aria-hidden size={20} strokeWidth={2.1} {...props} />;
}
