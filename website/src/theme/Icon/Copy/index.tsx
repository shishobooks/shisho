import { Copy } from "lucide-react";
import type { SVGProps } from "react";

export default function IconCopy(props: SVGProps<SVGSVGElement>) {
  return <Copy aria-hidden size={18} strokeWidth={2.1} {...props} />;
}
