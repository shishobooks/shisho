import { House } from "lucide-react";
import type { SVGProps } from "react";

export default function IconHome(props: SVGProps<SVGSVGElement>) {
  return <House aria-hidden size={18} strokeWidth={2.1} {...props} />;
}
