# Documentation Site

## Mission and Audience

Shisho's documentation helps readers decide whether Shisho fits their library, deploy it safely, operate it confidently, and use its supported extension points. Write for these audiences in priority order:

1. Prospective deployers evaluating Shisho
2. Operators and administrators responsible for an installation
3. Regular users managing and reading books
4. Readers configuring integrations
5. Plugin developers

Lead with the reader's task and the consequence of each choice. The docs are a product manual, not a record of the codebase.

## Site Facts

| Item | Value |
|------|-------|
| Framework | Docusaurus 3 |
| Source | `website/` |
| Current docs | `website/docs/`, labeled Unreleased |
| Canonical URL | `https://www.shishobooks.com` |
| `baseUrl` | `/`, in every environment |
| Local server | `mise docs` |
| Build | `cd website && pnpm build` |
| Theme E2E | `mise e2e:docs` |
| Deployment | GitHub Pages through `.github/workflows/docs.yml` |
| Deploy trigger | Push to `master` touching `website/**` or `.github/workflows/docs.yml`, or manual dispatch |
| Sidebar source | Explicit `docsSidebar` in `website/sidebars.ts` |

GitHub Actions installs dependencies, builds `website/`, uploads `website/build`, and deploys it with `actions/deploy-pages`. Do not describe a repository-path deployment URL or a variable base path.

## Canonical Information Architecture

`website/sidebars.ts` is authoritative. Ordering must never depend on `sidebar_position`, category metadata, filesystem order, or an autogenerated sidebar.

The canonical sidebar is:

1. **Getting Started**: safe first deployment and initial library setup
2. **Supported Formats**: evaluation of native input, reading, and download capabilities
3. **Using Shisho**: regular user tasks such as browsing, search, reading, playback, bulk actions, book and file management, metadata, and lists
4. **Administration**: deployment and maintenance, configuration, libraries, access control, installed plugins, supplements, and sidecars
5. **Integrations**: OPDS, Kobo Sync, and the eReader Browser
6. **Troubleshooting**: a prominent cross-cutting router from symptoms to canonical task pages
7. **Developer**: plugin development, manifest and hook contracts, host API reference, testing, and publishing

The exact category behavior is intentional:

- **Using Shisho** starts expanded because it contains common daily tasks.
- **Administration**, **Integrations**, and **Developer** start collapsed to keep the initial scan concise.
- **Developer** remains last unless the information architecture intentionally changes.
- **Troubleshooting** remains top-level and prominent because urgent problems cross audience and category boundaries.

### Ordering Rule

Place every new page where its intended reader would seek it. Never append a page to the bottom by default. Compare its place in the user journey and its importance with neighboring pages, then update `website/sidebars.ts` intentionally.

Optional or niche does not automatically mean last. A narrowly scoped prerequisite may belong early, while a broad reference may belong later. If a page changes the category's purpose or audience, reconsider the architecture instead of forcing it into the nearest directory.

### Stale Page Inventory

These current-doc files are legacy fragments omitted from the canonical sidebar. Do not restore them to navigation. Consolidate useful user-facing material into the named canonical owner, preserve any public URL before deletion, and remove duplication:

| Stale file | Canonical owner |
|------------|-----------------|
| `advanced/bulk-download.md` | `browsing-search-bulk-actions.md` |
| `audible-chapters.md` | `metadata.md` |
| `cache-management.md` | `deployment-and-maintenance.md` |
| `directory-structure.md` | `libraries.md` |
| `file-fingerprints.md` | `libraries.md` |
| `gallery-size.md` | `browsing-search-bulk-actions.md` |
| `gallery-sort.md` | `browsing-search-bulk-actions.md` |
| `review-state.md` | `metadata.md` |
| `plugins/repositories.md` | `plugins/overview.md` and `plugins/publishing.md` |

`advanced/` is not a documentation category. There is no rule that advanced or optional material belongs last.

## Editorial Policy

### Inclusion Test

Document a fact only when all five statements are true:

1. It is shipped in the version being documented.
2. It is relevant to a user, operator, integration owner, or plugin developer.
3. It teaches something beyond obvious labels in the interface.
4. It has one canonical page that owns the fact.
5. It describes a stable behavior or public contract that readers can rely on.

If one statement is false, leave the detail out or wait until the feature and contract are ready.

### Exclusions

Do not publish:

- Roadmaps, planned features, speculation, or design proposals
- Internal architecture that does not change a supported user task
- Database schemas, package layout, migration details, or test implementation
- Unsupported endpoints, private APIs, or incidental implementation behavior
- Changelog-style narration of how a feature evolved
- Links to any `CLAUDE.md` as user documentation

Link to an external technical source only when it is part of a supported contract or necessary context. Never send users to internal package notes for product guidance.

### Page, Section, and Category Tests

Create a page when a task has a distinct reader goal, enough durable content to stand alone, and a useful URL to link from support or the interface. Add a section when the material supports the same goal and audience as its parent page. Create or change a category only when several pages share a clear audience and purpose that the current architecture cannot express.

Do not create landing pages that only repeat the sidebar. Category labels and their first task page should provide orientation without a redundant legacy landing page.

Split mixed-audience content. Put operator setup and security decisions in Administration, daily workflows in Using Shisho, integration setup in Integrations, and plugin contracts in Developer. Cross-link at the decision point rather than copying instructions.

### Technical Detail Standard

Include enough technical detail for a reader to make a decision, verify state, or recover from failure. Prefer exact supported values, paths, UI labels, and observable outcomes. Omit implementation mechanics that do not alter those actions.

Do not infer behavior from a type name, route, or implementation alone. Verify user-facing claims against the interface, configuration reference, tests, and implementation as needed. Do not invent commands, recovery behavior, compatibility, defaults, or guarantees.

### Canonical Ownership and Duplication

Every fact has one canonical owner. Other pages summarize only the context needed to route the reader, then link to the owner. The homepage evaluates and routes; it is not the canonical deployment guide. Troubleshooting diagnoses and routes; it does not become a second configuration reference.

Before adding content, search current docs for the concept and choose the owner. When moving content, update inbound links and remove stale copies. Repeated text that can drift is a defect.

### UI Writing

Use the exact current UI label and path, with bold text for controls and navigation. Describe navigation with `>` separators, for example **Settings > Libraries**. State required permissions when access may differ by role. Do not claim an action is automatic when it requires a setting, plugin, job, or user choice.

### Destructive and Security-Sensitive Actions

Place a warning immediately before a destructive or security-sensitive step. State:

- What data, files, credentials, or access can change
- Whether the action is reversible
- What the reader should back up or verify first
- The safest alternative when one exists

Never bury file moves, deletion, permission expansion, secret rotation, remote access, or cache removal in a general note. Distinguish database deletion from files on disk. Distinguish read access from write access. Do not print real secrets in examples.

### Troubleshooting Format

Troubleshooting entries use this order:

1. **Symptom**: what the reader observes
2. **Likely cause**: the smallest set of supported explanations
3. **Verify**: a safe observation that separates those explanations
4. **Fix**: the least destructive supported correction, with a link to the canonical page

Do not start with broad cleanup or deletion. Preserve evidence such as job logs and current-session server logs until the cause is understood.

### Titles, Links, and URLs

Page titles use Title Case. Capitalize principal words and lowercase articles, coordinating conjunctions, and short prepositions. Preserve branded casing such as eReader, iPhone, and macOS.

When changing a title, grep current docs for its old display text and update references. Preserve an established URL unless a redirect is deliberately added and verified. Prefer relative links between docs. Use fragment links only after checking the rendered heading ID. Broken links fail the Docusaurus build.

## Versioned Docs Policy

Current work belongs in `website/docs/` and `website/sidebars.ts`. Do not edit `website/versioned_docs/` or `website/versioned_sidebars/` to backport current behavior. They are frozen release snapshots.

`scripts/release.sh` runs `pnpm docs:version X.X.X`, snapshots current docs and sidebars, updates `versions.json`, and commits the generated version with the release. The latest released version serves at `/docs/...`; current unreleased docs serve at `/docs/unreleased/...`; older versions serve at `/docs/X.X.X/...`.

Only correct a versioned page when it was factually wrong for that release, and treat that as an explicit maintenance task. A behavior that changed later is not a historical documentation error.

## Adding or Changing Documentation

1. Read this file, the root `CLAUDE.md`, the canonical owner page, and related pages completely.
2. Confirm the inclusion test and identify the primary audience and task.
3. Search current docs for duplicate claims, inbound links, exact UI text, and established URLs.
4. Verify behavior against shipped code and tests when the existing docs are insufficient.
5. Create or update the canonical page. Split mixed audiences and add focused cross-links.
6. Insert any new page in `website/sidebars.ts` according to reader journey and importance. Never rely on frontmatter or filesystem order.
7. Preserve old URLs or arrange an intentional redirect before removing a published page.
8. Update related current-doc links and homepage routing when the reader journey changes.
9. Run focused formatting or type checks while editing.
10. Run the docs build and `mise e2e:docs` before completion when dependencies and time allow.

### Verification Checklist

- [ ] Claims pass the inclusion test and describe shipped behavior
- [ ] One canonical page owns each fact
- [ ] UI labels, settings, defaults, paths, permissions, and warnings are exact
- [ ] No roadmap, internal implementation, unsupported API, or changelog narration leaked in
- [ ] Mixed audiences are split and cross-linked
- [ ] New pages were intentionally inserted in `website/sidebars.ts`
- [ ] `grep` found and resolved stale title, path, and cross-link references
- [ ] Established URLs were preserved or redirected intentionally
- [ ] Current docs were changed without editing versioned snapshots
- [ ] `cd website && pnpm build` passes
- [ ] `mise e2e:docs` passes when theme or navigation behavior is affected

## Stable Site Structure

```text
website/
├── docs/                  # Current Unreleased content
│   └── plugins/           # Plugin operator and developer content
├── versioned_docs/        # Frozen release snapshots
├── versioned_sidebars/    # Frozen sidebar snapshots
├── versions.json          # Released documentation versions
├── sidebars.ts            # Explicit current sidebar and ordering
├── docusaurus.config.ts   # URL, versioning, theme, and plugin config
├── e2e/                   # Documentation browser tests
├── src/
│   ├── pages/index.tsx    # Evaluation and routing homepage
│   ├── css/custom.css     # Theme overrides
│   └── theme/             # Swizzled Docusaurus components
└── static/img/            # Site icons and social assets
```

## Tooling and Theme

- `mise setup` installs both root and website dependencies. Use it for a new worktree.
- `mise docs` starts the local Docusaurus server.
- Root `pnpm lint:types` includes `pnpm -C website typecheck`.
- CI builds the docs and runs the theme browser checks.
- Prettier ignores generated `website/build/` and `website/.docusaurus/` output.
- The site is dark mode only with a violet Aurora theme.
- Geist, Noto Sans, and Noto Sans JP are the configured fonts.
- Swizzled Docusaurus icons use `lucide-react`, including custom admonition icons.
- The navbar logo combines the shelf mark, Shisho name, and 司書 superscript.
- `website/e2e/theme.spec.ts` checks control contrast, mobile drawer background, and the dark-only invariant.

## Icon and `.gitignore` Gotcha

The macOS `Icon\r\r` ignore pattern conflicts with swizzled `Icon/` directories. The root `.gitignore` has explicit exceptions:

```gitignore
!website/src/theme/Icon/
!website/src/theme/Icon/**
!website/src/theme/Admonition/Icon/
!website/src/theme/Admonition/Icon/**
```

Add matching exceptions before creating another swizzled icon directory, or Git may silently ignore it.

## Release and Deployment Gotchas

- Do not run `docusaurus deploy` manually. The GitHub Pages workflow owns deployment.
- The canonical URL is `https://www.shishobooks.com` and `baseUrl` is always `/`.
- A push deploys only after the workflow builds successfully, so broken links block publication.
- Release versioning snapshots both docs and sidebars. Confirm explicit sidebar changes before a release.
- Do not edit generated build output or frozen release snapshots for current behavior.
